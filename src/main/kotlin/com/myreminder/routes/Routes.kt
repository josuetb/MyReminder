package com.myreminder.routes

object Routes {
    const val BASE_URL = "/api/myreminder"

    // USERS
    const val USERS = "${BASE_URL}/users"

    // REMINDERS
    const val REMINDERS = "${BASE_URL}/reminders"

    // SUBJECTS
    const val SUBJECTS = "${BASE_URL}/subjects"
}
